# P1-04 Toon Ramp

Ramp is now an importable asset (`src/assets/toon_ramp.png`).
WGSL samples via `toonRampTexture` + `toonRampSampler`.
Fallback procedural is kept for offline builds without the PNG.
